import React from "react";

const Footer = () => (
  <footer className="bg-light text-center py-3 mt-5 border-top">
    <p className="mb-0 text-muted">© {new Date().getFullYear()} Art Gallery — Built with React + Node</p>
  </footer>
);

export default Footer;
